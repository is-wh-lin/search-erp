# search-erp

Public deployment shell for `https://erp.whlin.tw/`.

This repository does not contain the application source code. The public site is
served from the `gh-pages` branch, which contains build artifacts published from
the private source repository.

